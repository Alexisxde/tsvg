---
import IconsList from "@/components/icons-list"
import Layout from "@/layouts/Layout.astro"
import { getTeamsIcons } from "@/server/queries/team"

const icons = await getTeamsIcons()
---

<Layout>
	<IconsList {icons} client:only />
</Layout>
